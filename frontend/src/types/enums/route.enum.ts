export enum Route {
  DASHBOARD = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  RESET = "/reset",
  SET = "/set",

  PROFILE = "/profile",
  PASSWORD = `${PROFILE}/password`,

  COMPANIES = "/companies",

  ACTIONS = "/actions",

  USER = "/users",
}
